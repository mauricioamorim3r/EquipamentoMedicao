# Database Configuration
# Para desenvolvimento local, você pode usar uma das seguintes opções:

# Opção 1: PostgreSQL local (se você tiver PostgreSQL instalado localmente)
# DATABASE_URL="postgresql://username:password@localhost:5432/sgm_dev"

# Opção 2: Neon Database (serverless PostgreSQL - recomendado)
# DATABASE_URL="postgresql://username:password@ep-xxxxx.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Opção 3: Para testing rápido (SQLite - pode não funcionar com todas as features)
# DATABASE_URL="file:./dev.sqlite"

# IMPORTANTE: 
# 1. Substitua os valores acima pelos seus dados reais de conexão
# 2. Para produção, use o Neon Database conforme configurado no sistema
# 3. Este arquivo não deve ser commitado no git (já está no .gitignore)

# 🗄️ NEON DATABASE CONFIGURATION
# 
# ⚠️  SUBSTITUA a linha abaixo pela sua connection string real do Neon:
# 1. Acesse: https://neon.tech
# 2. Crie conta gratuita
# 3. Crie projeto: SGM-Database
# 4. Copie a connection string do dashboard
# 5. Cole aqui substituindo a linha abaixo:

DATABASE_URL="postgresql://neondb_owner:npg_yJFb6qitd0CK@ep-wild-firefly-ae98acu5-pooler.c-2.us-east-2.aws.neon.tech/sgm_production?sslmode=require&channel_binding=require"

# ✅ CONFIGURADO COM SUCESSO!
# Banco: sgm_production no Neon Database
# Região: US East 2 (Ohio)
# SSL: Habilitado com channel binding