# Configurações de Ambiente para Produção no Render
# Copie este arquivo para .env e configure as variáveis

# Ambiente da aplicação
NODE_ENV=production
PORT=10000

# URL da aplicação (será fornecida pelo Render)
APP_URL=https://your-app-name.onrender.com

# Banco de dados PostgreSQL
# Use a URL do banco PostgreSQL fornecida pelo Render
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Configurações de Sessão
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Configurações CORS (opcional)
CORS_ORIGIN=https://your-app-name.onrender.com

# Configurações de Upload (opcional)
MAX_FILE_SIZE=10mb
UPLOAD_DIR=uploads

# Configurações de Email (se necessário)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Configurações de Cache (opcional)
REDIS_URL=redis://localhost:6379

# Configurações de Log
LOG_LEVEL=info